import { createDemoAdapter } from "@/lib/retailers/createDemoAdapter";

// DEMO adapter — replace with a licensed Sainsbury's feed adapter implementing RetailerAdapter.
export const adapter = createDemoAdapter("sainsburys");
