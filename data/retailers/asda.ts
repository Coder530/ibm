import { createDemoAdapter } from "@/lib/retailers/createDemoAdapter";

// DEMO adapter — replace with a licensed Asda feed adapter implementing RetailerAdapter.
export const adapter = createDemoAdapter("asda");
