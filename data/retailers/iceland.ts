import { createDemoAdapter } from "@/lib/retailers/createDemoAdapter";

// DEMO adapter — replace with a licensed Iceland feed adapter implementing RetailerAdapter.
export const adapter = createDemoAdapter("iceland");
