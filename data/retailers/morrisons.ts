import { createDemoAdapter } from "@/lib/retailers/createDemoAdapter";

// DEMO adapter — replace with a licensed Morrisons feed adapter implementing RetailerAdapter.
export const adapter = createDemoAdapter("morrisons");
