import { createDemoAdapter } from "@/lib/retailers/createDemoAdapter";

// DEMO adapter — replace with a licensed Aldi feed adapter implementing RetailerAdapter.
export const adapter = createDemoAdapter("aldi");
