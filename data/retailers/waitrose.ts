import { createDemoAdapter } from "@/lib/retailers/createDemoAdapter";

// DEMO adapter — replace with a licensed Waitrose feed adapter implementing RetailerAdapter.
export const adapter = createDemoAdapter("waitrose");
