import { createDemoAdapter } from "@/lib/retailers/createDemoAdapter";

// DEMO adapter — replace with a licensed Ocado feed adapter implementing RetailerAdapter.
export const adapter = createDemoAdapter("ocado");
