import { createDemoAdapter } from "@/lib/retailers/createDemoAdapter";

// DEMO adapter — replace with a licensed Lidl feed adapter implementing RetailerAdapter.
export const adapter = createDemoAdapter("lidl");
