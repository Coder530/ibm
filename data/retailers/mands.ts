import { createDemoAdapter } from "@/lib/retailers/createDemoAdapter";

// DEMO adapter — replace with a licensed M&S Food feed adapter implementing RetailerAdapter.
export const adapter = createDemoAdapter("mands");
