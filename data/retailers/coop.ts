import { createDemoAdapter } from "@/lib/retailers/createDemoAdapter";

// DEMO adapter — replace with a licensed Co-op feed adapter implementing RetailerAdapter.
export const adapter = createDemoAdapter("coop");
