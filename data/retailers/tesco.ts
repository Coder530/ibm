import { createDemoAdapter } from "@/lib/retailers/createDemoAdapter";

// DEMO adapter — replace with a licensed Tesco feed adapter implementing RetailerAdapter.
export const adapter = createDemoAdapter("tesco");
